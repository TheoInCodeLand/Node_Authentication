<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TheoInCodeLand | Forgot Password</title>
    <link rel="stylesheet" href="/css/login.css">
</head>
<body>
    <div class="container">
        <div class="__logo">
            <img src='../assets/img/logo_2.png' width="200px" alt="">
        </div>

        <form class="form" action="/auth/forgot-password" method="post">
            <div class="flex-column">
                <label>Email</label>
                <% if (error) { %>
                    <div class="error-message">
                        <small style="color: #F14336;"><%= error %></small>
                    </div>
                <% } %>
            </div>
            <div class="inputForm">
                <input 
                    placeholder="Enter your Email" 
                    class="input" 
                    type="email" 
                    name="email" 
                    id="email" 
                    required 
                />
            </div>

            <button class="button-submit">Send Reset Link</button>

            <p class="p">Remember your password? <a href="/Auth/sign-in" class="span">Log In</a>.</p>
        </form>
    </div>
</body>
</html>
